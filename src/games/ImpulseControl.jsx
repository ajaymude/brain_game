import React, { useState } from 'react'; import GameLayout from '../components/GameLayout'; const ImpulseControl = ({ onBack }) => { const [show, setShow] = useState(false); const [target, setTarget] = useState(''); const [score, setScore] = useState(0); const [feedback, setFeedback] = useState(''); const [active, setActive] = useState(false); const startTest = () => { setActive(true); setShow(false); setTimeout(() => { const isGo = Math.random() < 0.7; setTarget(isGo ? 'GO' : 'STOP'); setShow(true); }, Math.random() * 2000 + 1000); }; const handleClick = () => { if (!show) return; if (target === 'GO') { setFeedback('✅'); setScore(score + 5); } else { setFeedback('❌ Should not click!'); setScore(Math.max(0, score - 3)); } setTimeout(() => { setFeedback(''); startTest(); }, 1000); }; return (<GameLayout title="Impulse Control" onBack={onBack} score={score} instructions="Click on GO, but NOT on STOP!"><div style={{ maxWidth: '700px', margin: '0 auto', display: 'flex', flexDirection: 'column', gap: 'var(--spacing-xl)', alignItems: 'center', minHeight: '500px', justifyContent: 'center' }}>{!active ? <button className="btn btn-primary" onClick={startTest}>Start Test</button> : <><div className="glass-card" style={{ width: '300px', height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'pointer', fontSize: '4rem', fontWeight: '900', background: show ? (target === 'GO' ? 'rgba(16,185,129,0.3)' : 'rgba(239,68,68,0.3)') : 'var(--glass-bg)' }} onClick={handleClick}>{show && target}</div>{feedback && <div style={{ fontSize: '2rem' }}>{feedback}</div>}</>}</div></GameLayout>); }; export default ImpulseControl;
